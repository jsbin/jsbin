// var $revert = $('#revert'); 

// $(document).bind('codeChange', function updateTitle(event, data) {
//   if (data.panelId === 'html') {
//     var title = !documentTitle ? 'JS Bin' : documentTitle;
//     document.title = title;
//   }
// });
